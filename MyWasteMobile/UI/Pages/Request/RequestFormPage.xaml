<?xml version="1.0" encoding="utf-8" ?>
<pages:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:MyWasteMobile.UI.Pages;assembly=MyWasteMobile"
             xmlns:controls="clr-namespace:MyWasteMobile.UI.Controls"
             x:Class="MyWasteMobile.UI.Pages.Request.RequestFormPage"
			 Title="Новая заявка">

	<ContentPage.Content>
		<StackLayout>
			<Label Text="RequestFormPage"/>
			<Button Text="InformationPage" Command="{Binding GoToInformationCommand}"/>
			<Button Text="MapPage" Command="{Binding GoToMapCommand}"/>
			<Button Text="RequestFormPage" Command="{Binding GoToRequestFormCommand}"/>

			<Button Text="RequestHistoryPage" Command="{Binding GoToRequestHistoryCommand}"/>
		</StackLayout>
	</ContentPage.Content>
</pages:BasePage>